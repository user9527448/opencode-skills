# Code Complexity Optimizer

> 通过复杂度分析进行系统性算法优化的 OpenCode 技能。

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/MIT)
[![OpenCode Compatible](https://img.shields.io/badge/OpenCode-Compatible-blue.svg)](https://opencode.ai)

## 概述

分析时间/空间复杂度（O(n)、O(log n)、O(n²) 等），通过交互式问答指导优化，并提供清晰的权衡建议。

**触发词：** "代码优化"、"code optimization"、"complexity optimization"、"性能优化"

## 功能特性

- 📊 **复杂度分析** - 自动检测时间/空间复杂度模式
- 💬 **交互式问答** - 明确优化目标（时间/空间/平衡）
- 🔄 **自动决策** - 基于约束的智能优化选择
- 🛠️ **工具集成** - 基准测试和验证脚本
- 🌍 **多语言** - Python、JavaScript、Java、C/C++、Go 指南
- 🎯 **多范式** - OOP、FP、Reactive、并发模式

## 目录结构

```
code-complexity-optimizer/
├── SKILL.md                    # 核心指令 (357 行)
├── SKILL-zh-cn.md              # 中文版本
├── README.md                   # 本文件
│
├── references/
│   ├── languages/              # 语言特定指南
│   │   ├── python.md
│   │   ├── javascript.md
│   │   ├── java.md
│   │   ├── cpp.md
│   │   └── go.md
│   └── paradigms/              # 范式特定模式
│       ├── oop.md
│       ├── functional.md
│       ├── reactive.md
│       └── concurrent.md
│
├── examples/                   # 优化模式
│   ├── time-optimization.md    # O(n²)→O(n)，O(n)→O(log n)
│   ├── space-optimization.md   # O(n)→O(1) 模式
│   └── anti-patterns.md        # 常见错误
│
├── templates/
│   └── verification.py         # 正确性验证模板
│
└── scripts/
    ├── analyze.py              # 多语言复杂度分析器
    └── benchmark.py            # 性能基准测试工具
```

## 快速开始

### 安装

```bash
# 克隆或复制到你的 OpenCode 技能目录
cp -r code-complexity-optimizer ~/.config/opencode/skills/

# 或项目级别
cp -r code-complexity-optimizer your-project/.opencode/skills/
```

### 使用方法

在 OpenCode 中提及任何优化相关任务：

```
用户: 优化这段代码的代码优化
用户: This code is too slow, help optimize
用户: 这段代码的时间复杂度是多少？
```

### 辅助脚本

```bash
# 安装依赖
pip install lizard radon

# 分析复杂度
python scripts/analyze.py src/utils.py
python scripts/analyze.py ./src --format json --output report.json

# 基准测试性能
python scripts/benchmark.py my_module.py process_data --runs 100
python scripts/benchmark.py original.py func --compare optimized.py func
```

## 优化工作流

```
阶段 1: 分析
├── 识别目标代码
├── 计算当前复杂度（时间/空间）
└── 找到瓶颈

阶段 2: 明确
├── 明确优化目标（时间/空间/平衡）
└── 了解约束条件

阶段 3: 制定策略
├── 选择优化方法
└── 提出更改方案及权衡分析

阶段 4: 执行
├── 应用最小更改
└── 保留行为

阶段 5: 验证
├── 运行测试
├── 比较输出
└── 生成报告
```

## 快速参考

```
┌─────────────────────────────────────────────┐
│        复杂度优化                            │
├─────────────────────────────────────────────┤
│ 时间 ↓                                      │
│   嵌套循环    → 哈希映射 / 集合             │
│   重复计算   → 记忆化                       │
│   线性搜索   → 二分搜索（已排序）           │
│   完全扫描       → 提前退出                 │
├─────────────────────────────────────────────┤
│ 空间 ↓                                      │
│   新数组      → 原地修改                    │
│   完整列表   → 生成器/迭代器                │
│   深度递归   → 迭代                        │
│   对象拷贝   → 引用                        │
├─────────────────────────────────────────────┤
│ 平衡                                        │
│   错误数据结构 → 正确数据结构               │
│   朴素算法  → 最优算法                     │
└─────────────────────────────────────────────┘
```

## 示例

**输入：**
```python
def find_duplicates(arr):
    duplicates = []
    for i in range(len(arr)):
        for j in range(i + 1, len(arr)):
            if arr[i] == arr[j] and arr[i] not in duplicates:
                duplicates.append(arr[i])
    return duplicates
```

**代理分析：**
```
分析结果:
- 时间复杂度: O(n³) - 双重循环 O(n²) × not in 检查 O(n)
- 空间复杂度: O(n) - duplicates 数组
- 瓶颈: 嵌套循环 + 线性查找

优化方案建议:
1. 【优先时间】使用集合去重
   - 时间: O(n²) → O(n)
   - 空间: O(n) (增加一个集合)
   
2. 【平衡优化】使用 Counter
   - 时间: O(n)
   - 空间: O(n)
```

## 参考资料

| 类别 | 文件数 | 描述 |
|----------|-------|-------------|
| **语言** | 5 | Python、JS、Java、C++、Go 特定模式 |
| **范式** | 4 | OOP、FP、Reactive、并发模式 |
| **示例** | 3 | 时间/空间优化、反模式 |

## 集成

与其他 OpenCode 技能配合良好：

- `test-driven-debugging` - 验证优化正确性
- `safe-refactoring` - 复杂重构
- `code-review-guardian` - 优化后质量检查

## 限制

- 无法修改编译后的代码或二进制文件
- 一些优化需要运行时分析
- 硬件特定优化需要目标测试

## 许可证

MIT 许可证 - 参见 [LICENSE](../../LICENSE)

## 作者

user9527448

## 链接

- [OpenCode 文档](https://opencode.ai/docs/skills/)
- [主要技能仓库](https://github.com/user9527448/opencode-skills)
